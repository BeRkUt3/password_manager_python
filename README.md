# Password Manager
Password manager created using tkinter library in python. This app has all needed functions, that every password manager should have.
At first you should to run init.py to create all needed files and start data.
Then run main.py to start the app(default root password: "0000").
