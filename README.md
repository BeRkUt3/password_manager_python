# Python Tkinter Password Manager

A password manager created using the Tkinter library in Python. This app includes all the essential features that every password manager should have.
Setup Instructions:
1. Initialize the app:
   - `python3 init.py` (This will create all necessary files and initialize the data.)
2. Run the app:
   - `python main_file.py` (Default root password: "0000")
